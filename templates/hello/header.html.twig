<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path("hello")}}">Logo</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor02">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path("hello")}}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>

				<li class="nav-item">
					<a class="nav-link active" href="{{path("categories")}}">Catégories
					</a>
				</li>

				<li class="nav-item">
					<a class="nav-link active" href="{{path("contact")}}">Contact
					</a>
				</li>
				{# {% if app.user and is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('admin')}}">Visible qu'en admin</a>
					</li>
				{% endif %} #}
			</ul>
			{% if app.user %}
			<div class="nav-item p-2">
				<a class="btn btn-secondary my-2 my-sm-0" href="{{path('app_logout')}}">Se deconnecter</a>
			</div>
			{% else %}
			<div class="nav-item p-2">
				<a class="btn btn-secondary my-2 my-sm-0" href="{{path('app_register')}}">Créer un compte</a>
			</div>
			<div class="nav-item p-2">
				<a class="btn btn-secondary my-2 my-sm-0" href="{{path('app_login')}}">Se connecter</a>
			</div>
			{% endif %}
		</div>
	</div>
</nav>
